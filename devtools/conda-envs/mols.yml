name: mols_test
channels:
     - defaults
dependencies:
     - python=3.7
     - hjkgroup::openbabel=2.4
     - conda-forge::xtb
     - psi4::psi4
     - pip:
       - tensorflow
       - scikit-learn
       - pandas
       - scipy
     # GUI
     - qt
     - pyqt
       #- gpy # in pip the spelling is GPy, optional
       #- hyperopt # optional
     - theochem::iodata
